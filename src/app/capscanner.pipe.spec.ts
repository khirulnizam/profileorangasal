import { CapscannerPipe } from './capscanner.pipe';

describe('CapscannerPipe', () => {
  it('create an instance', () => {
    const pipe = new CapscannerPipe();
    expect(pipe).toBeTruthy();
  });
});
